# SukoonAI backend dependencies.
# Verified working on Python 3.13 (Windows, CPU) in the C:\skv venv.
# Install:  python -m pip install -r backend/requirements.txt

# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
python-dotenv>=1.0

# --- Text sentiment model (transformer) ---
torch>=2.2            # install CPU build via: --index-url https://download.pytorch.org/whl/cpu
transformers>=4.44

# --- Voice emotion model ---
tensorflow>=2.18      # provides keras 3.x
librosa>=0.10
soundfile>=0.12
audioread>=3.0
numba>=0.60
scikit-learn>=1.3     # label encoder unpickling
joblib>=1.3

# --- Fusion: speech-to-text ---
openai-whisper>=20231117

# --- Gemini AI ---
google-genai>=1.0

# Optional (training-time plotting only; not needed for serving):
# matplotlib>=3.7
# seaborn>=0.12
# pandas>=2.0
uvicorn[standard]